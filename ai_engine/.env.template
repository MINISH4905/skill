# AI Engine (FastAPI) - copy to ai_engine/.env
PORT=8001
DEBUG=True

# Optional: disable downloading/running Hugging Face models (templates only; fastest cold start)
SKILLFORGE_DISABLE_LLM=false

# Override model name when LLM is enabled
SKILLFORGE_MODEL_NAME=google/flan-t5-small
